# Investigative notebooks

Run the notebooks in numerical order. `00_data_inventory_and_quality.ipynb` is the gatekeeper: resolve provenance and source-site anomalies before combining datasets.

Notebooks write generated figures and audit tables to `../analysis/` and never modify files in `../database/`.
